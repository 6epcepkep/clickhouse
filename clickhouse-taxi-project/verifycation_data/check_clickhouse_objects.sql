-- Проверка содержимого таблицы/представления `routes`
SELECT * FROM routes LIMIT 10;

-- Проверка содержимого таблицы/представления `drivers`
SELECT * FROM drivers LIMIT 10;

-- Проверка содержимого таблицы/представления `vendors`
SELECT * FROM vendors LIMIT 10;

-- Проверка содержимого таблицы/представления `payment_types`
SELECT * FROM payment_types LIMIT 10;

-- Проверка содержимого таблицы/представления `trips_raw`
SELECT * FROM trips_raw LIMIT 10;

-- Проверка содержимого таблицы/представления `trips_by_hour`
SELECT * FROM trips_by_hour LIMIT 10;

-- Проверка содержимого таблицы/представления `trips_by_driver_name`
SELECT * FROM trips_by_driver_name LIMIT 10;

-- Проверка содержимого таблицы/представления `trips_by_payment_type`
SELECT * FROM trips_by_payment_type LIMIT 10;

-- Проверка содержимого таблицы/представления `revenue_by_vendor`
SELECT * FROM revenue_by_vendor LIMIT 10;

-- Проверка содержимого таблицы/представления `trip_duration_distribution`
SELECT * FROM trip_duration_distribution LIMIT 10;

-- Проверка содержимого таблицы/представления `trip_duration_distribution_data`
SELECT * FROM trip_duration_distribution_data LIMIT 10;

-- Проверка содержимого таблицы/представления `avg_fare_by_route`
SELECT * FROM avg_fare_by_route LIMIT 10;

-- Проверка содержимого таблицы/представления `top_routes_last_week`
SELECT * FROM top_routes_last_week LIMIT 10;

-- Проверка содержимого таблицы/представления `revenue_by_vendor_last_15m`
SELECT * FROM revenue_by_vendor_last_15m LIMIT 10;

-- Проверка содержимого таблицы/представления `trips_by_payment_type_last_15m`
SELECT * FROM trips_by_payment_type_last_15m LIMIT 10;

-- Проверка содержимого таблицы/представления `avg_fare_by_route_last_15m`
SELECT * FROM avg_fare_by_route_last_15m LIMIT 10;

-- Проверка содержимого таблицы/представления `trips_by_driver_last_15m`
SELECT * FROM trips_by_driver_last_15m LIMIT 10;

-- Проверка содержимого таблицы/представления `trip_duration_distribution_last_15m`
SELECT * FROM trip_duration_distribution_last_15m LIMIT 10;
